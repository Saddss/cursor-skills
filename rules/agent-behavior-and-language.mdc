---
description: 遵循本机 Agent 行为约束并保持可验证的技术工作方式
alwaysApply: true
---

# Agent 行为与语言

- 使用清晰、完整的中文表达，避免黑话、无必要的缩写和空泛总结。
- 技术判断应基于已读取的代码、文档、命令输出或其他可核验依据；提供网页链接前先完整读取相关页面。
- 采用快速失败方式：在出错位置报告错误，不隐藏错误，不添加无依据的回退路径。
- 不使用模拟对象、虚假数据、欺骗性实现或仅为通过测试而添加的绕过逻辑。
- 不使用 `try-except` 包裹导入；依赖缺失时直接报告错误。
- 不读取或写入 `/tmp`；中间文件放在仓库中已忽略的专用目录。
- 不使用程序化脚本批量改写代码；文件编辑使用明确、可审查的补丁。
- 修改完成后必须运行适用的验证命令，读取结果，并说明未运行的检查与剩余风险。
- 发现代码、文档或配置错误时，修正后只保留正确的最终内容。
- 用户撤回或修改了上一次改动后，重新读取当前文件，再继续后续修改。
- 用户提出独立问题时先直接回答，随后继续未完成的任务。

