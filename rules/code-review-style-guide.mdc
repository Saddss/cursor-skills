---
description: 按 Issue #32 执行简洁、正确且可维护的代码审查
alwaysApply: true
---

# 代码审查规范

代码审查按以下顺序展开：技术背景与改动必要性、代码质量与正确性、目标完整性、性能、可维护性、测试和生成代码迹象。每个问题都要说明影响、原因、严重程度和可执行的修正方式；没有发现问题时也要说明检查范围和依据。

## 简洁性

- 优先减少文件、函数、抽象层和状态流转。
- 只调用一两次且很短的辅助函数直接写回调用位置；重复使用或确实提升可读性时才保留辅助函数。
- 暂无第二个实现时，不新增接口、基类、注册表或插件系统。
- 相关函数可以放在同一个文件中，避免为了形式上的单一职责制造文件碎片。
- 不重复实现标准库或已有依赖提供的能力。
- 删除无用代码、过期的 TODO、注释掉的代码和无法到达的分支。

## 正确性与错误处理

- 在公共接口和系统边界校验输入；内部不变量使用 `assert`，用户输入和环境错误使用明确的异常类型。
- 让错误在发生位置暴露。禁止宽泛捕获、空处理和无依据的回退。
- 每个 `try-except` 都必须能说明删除后会破坏什么；只捕获明确的异常并保持保护范围足够小。
- 不为几乎不会发生的错误增加防御代码和对应测试。
- 资源必须有明确的生命周期；缓存和缓冲区必须有容量边界或淘汰策略。
- 多线程代码明确线程安全保证，缩小锁范围，禁止在锁内执行输入输出或 GPU 操作。

## 命名、类型和结构

- 名称表达真实的数据含义、物理含义和单位，禁止使用无法解释的 `x`、`z`、`data`、`result` 等泛化名称。
- 公共接口和函数签名提供完整类型标注；禁止使用 `Any` 绕过类型检查，禁止使用裸 `dict`、`list` 和 `tuple`。
- 只在函数内部定义的嵌套函数使用 `_` 前缀；模块级名称、类名称、类成员和公共函数不使用无意义的 `_` 前缀。
- 类型已经确定时直接访问属性，禁止使用 `getattr` 和 `hasattr` 进行无依据的动态防御。
- 只使用一次的常量放在实际使用位置；跨文件传递配置时通过参数传入，避免单次导入常量。
- 函数保持在一次阅读范围内；过长函数拆分时必须让拆分后的结构真正提升理解和复用。
- 导入按照标准库、第三方依赖、本地模块分组，禁止通配符导入、路径注入和无必要的函数内部导入。

## 性能

- 推理和训练热路径避免频繁调用 `.item()`、`.cpu()`、`.tolist()` 和 Python 层循环。
- 优先使用向量化 GPU 操作，检查不必要的主机同步、设备同步、内存分配和通信等待。
- 常量和经验参数使用有含义的名称，并记录必要的来源、测量条件和单位。

## 注释、文档字符串和日志

- 每条注释都应说明维护者确实需要知道的原因、约束、权衡或外部依据；无法说明原因的注释直接删除。
- 跨团队协作的代码注释使用仓库既有语言；新增注释必须简洁，并按仓库约定标注作者，例如 `# Note (name): ...`。
- Python 行内注释和文档字符串不使用 Markdown 反引号包裹代码标识符。
- 禁止添加过程标记、装饰性横线、无工单的 TODO、手工日志等级前缀和工具生成痕迹。
- 日志使用仓库既有日志库和统一格式，禁止手工添加 `[Info]`、`[Warn]` 等前缀。

## 测试与验证

- 新增逻辑必须有覆盖输入到输出契约的测试；测试应覆盖真实错误路径，避免为无意义的防御代码编写测试。
- 测试保持确定性，固定随机种子，避免测试内部实现细节。
- 运行仓库已有格式检查、静态检查、类型检查和测试命令；审查结论明确记录已运行和未运行的检查。

