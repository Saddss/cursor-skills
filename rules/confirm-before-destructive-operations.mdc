---
description: 终止进程、停止容器或删除数据前确认具体目标和影响
alwaysApply: true
---

# 破坏性操作确认

- 在终止进程、停止容器或删除数据前，先确认具体目标和影响。
- 当操作可能影响运行中的容器、未保存工作、共享资源，或因磁盘不足需要清理非临时数据时，必须取得用户对具体对象的明确同意。
- 用户已经明确指定对象和操作时，不重复询问；目标仍不明确时，先报告现状并提供非破坏性替代方案。
- 任何可能重写或丢弃 Git 工作树、提交历史或用户数据的修正，都先建立可恢复点并确认范围。
