---
description: 杀进程或清理磁盘前必须先询问用户（容器运行中 / 磁盘不足时）
alwaysApply: true
---

# 杀进程与清理磁盘前必须确认

无论当前任务多紧急，遇到以下情况且需要 **kill 进程** 或 **清理磁盘** 时，必须先停下来询问用户，得到明确同意后再执行：

## 触发条件（任一满足即适用）

1. **有容器在运行**：检测到 Docker / Podman / containerd 等容器正在运行（含 `docker ps` 非空、compose 服务 up 等）
2. **磁盘空间不足**：当前分区可用空间紧张，或任务因空间不足可能/已经失败

## 必须先询问的操作

- `kill` / `pkill` / `killall` 终止进程
- `docker stop` / `docker rm` / `docker compose down` 等停止或删除容器
- 删除文件或目录以释放空间（含 `rm -rf`、清理 cache、日志、镜像、volume 等）
- 任何等效的破坏性清理或终止操作

## 执行要求

1. **先报告**：说明检测到的容器/磁盘状况，以及你打算做什么、会影响什么
2. **等待确认**：用户明确同意后再执行；未确认则不得自行 kill 或清理
3. **提供替代方案**：可建议更安全的路径（如换目录、缩小 scope），但不代替用户做决定

## 例外

无。即使用户任务描述里隐含了「清理」或「重启」，也仍需先确认具体要杀/删的对象。
