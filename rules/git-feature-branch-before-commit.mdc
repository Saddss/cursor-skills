---
description: Git 提交前必须先切 feature 分支，禁止直接在 main 上 commit
alwaysApply: true
---

# Git：先切分支再提交

在用户的 git 仓库里创建 commit 之前，**必须先**：

1. 确认当前不在 `main` / `master`（或用户指定的默认保护分支）上；若已在上面且有未提交改动，先 `git checkout -b <branch>`。
2. 分支名要有语义，例如 `fix/gemma4-mixed-precision-moe`，不要用无意义的 `temp`。
3. 提交完成后，`main` 应保持与 `origin/main` 对齐（除非用户明确要求 merge 到 main）。

## 禁止

- 直接在 `main` 上 `git commit` 然后把「ahead N」留给用户自己收拾。
- 提交前不说明当前分支名。

## 标准流程

```bash
git fetch origin
git checkout -b fix/<topic> origin/main   # 或从当前工作点建分支
# ... 改动 ...
git add <files>
git commit -m "..."
git status -sb   # 确认在 feature 分支，main 未被污染
```

## 汇报

commit 完成后告诉用户：**分支名**、**commit hash**、**是否已 push**；若 `main` 曾被误 commit，说明已如何用 `git branch` + `git reset --hard origin/main` 修正。
