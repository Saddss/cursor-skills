---
description: 强制执行本机全局 AGENTS.md 中的语言、行为、验证和安全规则
alwaysApply: true
---

# 全局代理规则

执行任何任务前，必须读取并遵循本机 `$CODEX_HOME/AGENTS.md` 中的全部强制规则。macOS 默认路径为 `~/.codex/AGENTS.md`。该文件是本机全局规则的唯一来源，规则内容发生变化时，以该文件的当前内容为准。

与其他 Cursor rules 和 skills 共同执行时：

- 保留现有规则中关于危险操作确认、分支提交、注释质量、验证结果和技能专用流程的要求。
- 用户全局规则约束语言表达、方案设计、代码编写、文件处理、验证方式和工具使用。
- 规则出现适用范围差异时，执行同时满足各项要求的做法；无法同时满足时，停止操作并说明具体冲突。
- 任何任务完成前，都必须执行对应的验证命令并读取验证结果。
