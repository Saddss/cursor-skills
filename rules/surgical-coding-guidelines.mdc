---
description: 采用简单、外科式、目标驱动且可验证的实现方式
alwaysApply: true
---

# 实现风格

- 对小且可逆的歧义采用合理假设并说明；只有会实质改变结果、权限或外部影响的歧义才停下来询问。
- 只实现请求范围，不添加推测性功能、一次性抽象或不必要的防御分支。
- 只修改必要代码，匹配现有风格，不顺手重构、格式化或清理无关区域。
- 清理由本次修改直接造成的未使用 import、变量、函数和其他孤立代码。
- 复杂、风险较高或包含多个实质步骤的任务，应先给出简短计划和可验证的成功条件；简单任务直接执行并做适度验证。
- 在合理范围内持续迭代直到验证完成；明确说明未运行的检查和残余风险。
